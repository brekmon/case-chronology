[pytest]
testpaths = tests
# The tools are single-file modules at the repository root. tests/conftest.py puts
# that root on sys.path, which is simpler than making the project a package purely
# to satisfy the runner.
addopts = -q --strict-markers
